import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  imports: [RouterLink],
  selector: 'app-first',
  templateUrl: './first.html',
  styleUrl: './first.scss',
})
export class First {}
